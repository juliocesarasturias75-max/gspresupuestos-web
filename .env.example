# Secreto para firmar tokens JWT (cámbialo por uno largo y aleatorio)
APP_JWT_SECRET=tu-secreto-muy-largo-y-aleatorio-aqui

# Cuenta administrador (se crea sola al arrancar si no hay usuarios)
ADMIN_EMAIL=admin@gspresupuestos.com
ADMIN_PASSWORD=GspAdmin2026!

# Opcional: desactivar login (solo para pruebas locales)
# AUTH_DISABLED=true

# Disco persistente en Render (mount path del disco)
# GSP_DATA_DIR=/var/data

# Python en Render
PYTHON_VERSION=3.11.11
